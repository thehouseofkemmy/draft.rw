# drafts.rw

drafts.rw is a shared platform for drafts — pieces, fragments, and quiet work. This project is built with Vite, React, TypeScript, Supabase, and Tailwind CSS.

## Run locally

- `npm install`
- `npm run dev`
- `npm run build`
